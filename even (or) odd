#include <stdio.h>
int main()
{
    int a;
    printf("enter a number:");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("%i is a even number",a);
    }
    else
    {
        printf("%i is a odd number",a);
    }
}
